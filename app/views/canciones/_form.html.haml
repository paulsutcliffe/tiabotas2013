= form_for @cancion, :html => { class: 'form-horizontal', role: 'fordb/migrate/20131119172026_create_diseños.rbm' } do |f|
  - if @cancion.errors.any?
    #error_explanation
      %h2= "#{pluralize(@cancion.errors.count, "error")}"
      %ul
        - @cancion.errors.full_messages.each do |msg|
          %li= msg

  .form-group
    = f.label :link, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :link, class: 'form-control'
  .actions
    = f.submit 'Guardar', class: 'btn btn-default'
